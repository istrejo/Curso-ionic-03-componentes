export interface Componente {
  icon: string;
  name: string;
  redirecTo: string;
}

export interface Album {
  userId: number;
  id: number;
  title: string;
}

export interface Superhero {
  superhero: string;
  publisher: string;
  alter_ego: string;
  first_appearance: string;
  characters: string;
}
